FROM golang:1.24

# This is not a real deployment Dockerfile, is it used only for assessment purpose, giving the environment to run the scripts.
# Is it not useful pre-builing stuff, since they are build via `build.sh` script.